[package]
name = "git-credential-nostr"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Git credential helper that produces Bearer auth headers for Buzz's git server"

[lib]
name = "git_credential_nostr"
path = "src/lib.rs"

[[bin]]
name = "git-credential-nostr"
path = "src/main.rs"

[dependencies]
# The bearer credential helper reads the API key from the environment and emits
# it verbatim — no Nostr signing, serialization, or base64 encoding required.
